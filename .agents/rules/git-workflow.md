# Git Workflow Rule

Do NOT automatically run `git push` to the remote repository. 
When making changes, add and commit the files locally using `git add` and `git commit`, but do not push. Always ask the user or inform them that the changes are committed locally and ready for them to push.
